export interface Experience {

    id?: number;
    title: string;
    place: string;
    startDate?: Date;
    endDate?: Date;
    description: string;
    userId: number;
    pictureUrl?: string;

}